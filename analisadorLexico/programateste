program 
	/* Aqui vai um comentário*/ /* contendo qualquer coisaewq*/
	integer : weight, group, charge, distance;
	char: cintia;
begin
	distance := -2300; 12.34 -1.34 436.342
	read weight;
	if weight > 60 then group :=5
				   else group := (weight + 14)/15
	endif;
	charge := 40 + 3 * (distance/1000)
	write charge
end
